<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.score.sts">

    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- === ACTIVITIES === -->
        <activity
            android:name=".presentation.view.activity.LandingActivity"
            android:label="@string/title_activity_entry"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="stateHidden" />

        <!-- SplashActivity page Activity -->
        <activity
            android:name=".presentation.view.activity.SplashActivity"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.view.activity.ProfileActivity"/>

        <activity android:name=".presentation.view.activity.FingerprintActivity"/>

        <activity android:name=".presentation.view.activity.ProfileDisplayActivity"/>
    </application>

</manifest>